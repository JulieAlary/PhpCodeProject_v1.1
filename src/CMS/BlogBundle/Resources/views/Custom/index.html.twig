{% extends 'CMSBlogBundle::layout.html.twig' %}


{% block cms_body %}
    <h2>Personnalisation de votre blog</h2>

    {#On affiche tous les messages flash dont le nom est « info » #}
    {% for message in app.session.flashbag.get('info') %}
        <div class="alert alert-info">Message flash : {{ message }}</div>
    {% endfor %}

    <div class="row">

        <div class="col-md-8">
            <div class="flip">
                <h3>
                    <button class="btn btn-info btn-xs pull-right"><i class="fa fa-info" aria-hidden="true"></i>&nbsp;&nbsp;Aide
                    </button>
                </h3>
            </div>

            <div class="panel-flip bg-info">
                <ul>
                    <li>Dans la rubrique "Identité du blog vous pouvez choisir un titre, une description ainsi qu'une
                        thème.
                    </li>
                    <li>Une fois ces options choisies, vous ne pourrez les modifier quand dans l'entete qui se créera
                        avec vos informations
                    </li>
                    <li>Vous allez pouvoir également créer et afficher un carousel ou slider qui se mettra en place sur
                        votre page d'accueil
                    </li>
                </ul>
            </div>

            <ul>
                {% if theme == null %}
                    <li><a href="{{ path('cms_custom') }}">Identité du blog &nbsp;&nbsp;<i
                                    class="fa fa-arrow-circle-right" aria-hidden="true"></i></a></li>
                {% else %}
                    <li title="Un seul thème à la fois!" class="pointer">Identité du blog &nbsp;&nbsp;</li>
                {% endif %}
                <li><a href="{{ path('cms_custom_carousel') }}">Carousel&nbsp;&nbsp;<i
                                class="fa fa-arrow-circle-right" aria-hidden="true"></i></a></li>
            </ul>
        </div>

    </div>
{% endblock %}



